<UserControl 
    x:Class="CourseProjectOpp.RegistrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:CourseProjectOpp"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis" />
        <local:SecureStringToCharEnumerableConverter x:Key="secureStrToChars" />
        <local:StringToCharEnumerableConverter x:Key="strToChars" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="55*" />
            <RowDefinition Height="96*" />
            <RowDefinition Height="74*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid
            Grid.Row="1"
            Grid.Column="1"
            Background="White">

            <StackPanel 
                Margin="10,10,10,10">

                <Label Content="Login"/>

                <TextBox 
                    MaxLength="16"
                    Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />

                <Label 
                    Content="{Binding InvalidLoginWarning, UpdateSourceTrigger=PropertyChanged}"
                    FontSize="10"
                    Foreground="Red"/>

                <Label Content="Password" />

                <DockPanel 
                    LastChildFill="True">

                    <CheckBox
                        x:Name="hidePasswordCheckBox"             
                        DockPanel.Dock="Right" />

                    <Grid
                        DockPanel.Dock="Left">

                        <PasswordBox                         
                            MaxLength="16" 
                            PasswordChar="•" >

                            <PasswordBox.Style>
                                <Style
                                    TargetType="PasswordBox">
                                    <Style.Triggers>
                                        <DataTrigger 
                                            Binding="{Binding IsChecked.Not, 
                                                              ElementName=hidePasswordCheckBox}" 
                                            Value="False">

                                            <Setter 
                                                Property="Visibility" 
                                                Value="Visible" />

                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </PasswordBox.Style>

                            <i:Interaction.Behaviors>
                                <local:PasswordBehavior 
                                    SecurePassword="{Binding Password, 
                                                             Mode=TwoWay, 
                                                             Converter={StaticResource secureStrToChars}}" />
                            </i:Interaction.Behaviors>

                        </PasswordBox>


                        <TextBox
                            Visibility="{Binding IsChecked, 
                                                 ElementName=hidePasswordCheckBox, 
                                                 Converter={StaticResource boolToVis}}">
                            <TextBox.Style>
                                <Style
                                    TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger
                                            Property="Visibility"
                                            Value="Visible">

                                            <Setter
                                                Property="Text"
                                                Value="{Binding Password, Converter={StaticResource strToChars}}" />


                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Grid>
                </DockPanel>

                <Label Content="Repeat password" />

                <DockPanel 
                    LastChildFill="True">

                    <CheckBox
                        x:Name="hideRepeatPasswordCheckBox"             
                        DockPanel.Dock="Right" />

                    <Grid
                        DockPanel.Dock="Left">

                        <PasswordBox                         
                            MaxLength="16" 
                            PasswordChar="•" >

                            <PasswordBox.Style>
                                <Style
                                    TargetType="PasswordBox">
                                    <Style.Triggers>
                                        <DataTrigger 
                                            Binding="{Binding IsChecked.Not, 
                                                              ElementName=hideRepeatPasswordCheckBox}" 
                                            Value="False">

                                            <Setter 
                                                Property="Visibility" 
                                                Value="Visible" />

                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </PasswordBox.Style>

                            <i:Interaction.Behaviors>
                                <local:PasswordBehavior 
                                    SecurePassword="{Binding RepeatedPassword, 
                                                             Mode=TwoWay, 
                                                             Converter={StaticResource secureStrToChars}}" />
                            </i:Interaction.Behaviors>

                        </PasswordBox>


                        <TextBox
                            Visibility="{Binding IsChecked, 
                                                 ElementName=hideRepeatPasswordCheckBox, 
                                                 Converter={StaticResource boolToVis}}">
                            <TextBox.Style>
                                <Style
                                    TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger
                                            Property="Visibility"
                                            Value="Visible">

                                            <Setter
                                                Property="Text"
                                                Value="{Binding RepeatedPassword, Converter={StaticResource strToChars}}" />


                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Grid>
                </DockPanel>

                <Label 
                    x:Name="invalidPasswordLabel"
                    Content="{Binding InvalidPasswordWarning, UpdateSourceTrigger=PropertyChanged}"
                    Foreground="Red"
                    FontSize="10"/>

                <Button
                    Content="Sign in"
                    HorizontalAlignment="Center"
                    Padding="2"
                    Command="{Binding RegistrationCommand}" />

            </StackPanel>
        </Grid>


    </Grid>
</UserControl>
